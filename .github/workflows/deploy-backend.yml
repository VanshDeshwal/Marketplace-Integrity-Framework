name: Deploy Backend API to Azure (Docker)

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - 'dataset/**'
      - '.github/workflows/deploy-backend.yml'
  repository_dispatch:
    types: [deploy-backend]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: marketplace-integrity-api
  REGISTRY: ghcr.io
  IMAGE_NAME: vanshdeshwal/marketplace-integrity-framework-backend

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile.prod
        push: true
        tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
        labels: |
          org.opencontainers.image.source=${{ github.event.repository.html_url }}
          org.opencontainers.image.revision=${{ github.sha }}

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Update Container Image
      run: |
        az webapp config container set \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --resource-group marketplace-integrity-rg \
          --docker-custom-image-name ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
          --docker-registry-server-url https://${{ env.REGISTRY }}

    - name: Restart Web App
      run: |
        az webapp restart \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --resource-group marketplace-integrity-rg

    - name: Configure Azure App Settings
      run: |
        az webapp config appsettings set \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --resource-group marketplace-integrity-rg \
          --settings \
            CORS_ORIGINS="https://marketplace.vanshdeshwal.dev" \
            LOG_LEVEL="INFO" \
            DEVELOPMENT="false" \
            AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
            WEBSITES_PORT="8000" \
            ENABLE_ORYX_BUILD="false" \
            SCM_DO_BUILD_DURING_DEPLOYMENT="false" \
            WEBSITES_ENABLE_APP_SERVICE_STORAGE="false"

    - name: Health Check
      run: |
        echo "Waiting for container deployment to be ready..."
        sleep 60
        curl -f https://api.marketplace.vanshdeshwal.dev/health || echo "Health check failed - container may still be starting"

    - name: Deployment Summary
      run: |
        echo "🚀 Backend Docker deployment completed!"
        echo "🐳 Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
        echo "🔗 API URL: https://api.marketplace.vanshdeshwal.dev"
        echo "🌐 Frontend URL: https://marketplace.vanshdeshwal.dev"
        echo "💾 Storage: Azure Blob Storage"
        echo "✅ Fixed lowercase repository name issue"
