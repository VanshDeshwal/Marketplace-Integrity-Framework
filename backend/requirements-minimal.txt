# Minimal production requirements - only what's actually used
fastapi==0.95.2
uvicorn==0.22.0
python-multipart==0.0.6
pydantic==1.10.9

# ML Core (CPU optimized)
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.2

# Image processing
Pillow==10.3.0

# ML Models (CPU only)
sentence-transformers==2.2.2
faiss-cpu==1.7.4
open_clip_torch==2.24.0

# PyTorch CPU-only (installed separately in Dockerfile for size optimization)
# torch==2.6.0+cpu
# torchvision==0.17.0+cpu
